{
  "private": true,
  "scripts": {
    "build": "tstl",
    "minify": "npx luamin -f ./out/BladeTS.lua > ./out/BladeTS.min.lua",
    "release": "npm run build && deno run --allow-read --allow-write --allow-run ./build/release.ts",
    "copy_to_addons": "robocopy /E out \"%WOW_ADDON_DIR%/BladeTS/\"",
    "publish": "npm run release && npm run copy_to_addons"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ace3decl": "file:../ace3decl",
    "lua-types": "^2.13.0",
    "luamin": "^1.0.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4",
    "typescript-to-lua": "^1.10.1"
  }
}
